{
  "name": "users-registration-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node .",
    "watch": "nodemon .",
    "inspect": "node --inspect-brk .",
    "watch-inspect": "nodemon --inspect-brk .",
    "test": "mocha --timeout 5000",
    "test-inspect": "mocha --timeout 5000 --inspect *.spec.js",
    "test-inspect-brk": "mocha --timeout 5000 --inspect-brk *.spec.js",
    "test-coverage": "nyc --reporter=html --reporter=text mocha --timeout 5000 *.spec.js",
    "debug": "mocha --debug"
  },
  "keywords": [
    "users",
    "registration",
    "authentication",
    "api"
  ],
  "author": "Ismael García Luján",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.2",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "users-registration-data": "file:./users-registration-data",
    "users-registration-errors": "file:./users-registration-errors",
    "users-registration-logic": "file:./users-registration-logic",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^10.0.0",
    "nodemon": "^2.0.15",
    "nyc": "^15.1.0",
    "supertest": "^6.2.4"
  }
}
